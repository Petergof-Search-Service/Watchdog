# Base URL of the API, including the version prefix (no trailing slash).
API_BASE_URL=https://petergof-sciense-rag.ru/api/v1
# Shared service key — must match the API's CLOUD_FUNCTION_API_KEY.
CLOUD_FUNCTION_API_KEY=change-me
